# Design System Guide

The canonical design-system guide lives in [`packages/ui/DESIGN.md`](packages/ui/DESIGN.md).

Use that file for theme tokens, typography, palette rules, reusable component conventions, and the boundary between `@orbit/ui` and `apps/www`.
