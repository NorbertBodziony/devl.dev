// @ts-nocheck
import { createEffect, createSignal } from "solid-js"; import { cn } from "../../lib/utils";
export function Switch(props:any){ const [checked,setChecked]=createSignal(Boolean(props.checked ?? props.defaultChecked)); createEffect(()=>{ if(props.checked!==undefined) setChecked(Boolean(props.checked)); }); return <button type="button" role="switch" aria-checked={checked()} {...props} class={cn("inline-flex h-5 w-9 shrink-0 items-center rounded-full border border-transparent bg-input p-0.5 transition-colors data-[state=checked]:bg-primary", props.className, props.class)} data-state={checked()?"checked":"unchecked"} onClick={(e)=>{ props.onClick?.(e); const next=!checked(); setChecked(next); props.onCheckedChange?.(next); }}><span class={cn("block size-4 rounded-full bg-background shadow transition-transform", checked() && "translate-x-4")}/></button>; }
