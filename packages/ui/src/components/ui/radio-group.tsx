// @ts-nocheck
import { createSignal } from "solid-js"; import { Primitive } from "./_primitive"; import { cn } from "../../lib/utils";
export function RadioGroup(props:any){ return <Primitive role="radiogroup" {...props}/>; } export function Radio(props:any){ const [checked,setChecked]=createSignal(Boolean(props.checked ?? props.defaultChecked)); return <button type="button" role="radio" aria-checked={checked()} {...props} class={cn("inline-flex size-4 items-center justify-center rounded-full border border-input", props.className, props.class)} onClick={(e)=>{props.onClick?.(e); setChecked(true); props.onCheckedChange?.(true);}}>{checked()?<span class="size-2 rounded-full bg-primary"/>:null}</button>; }
